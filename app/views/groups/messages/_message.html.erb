<% if message.user_id == user.id %>
 <%= render partial: 'groups/messages/message_current_user', locals: { message: message } %>
<% else %>
  <%= render partial: 'groups/messages/message_other_user', locals: { message: message } %>
<% end %>
